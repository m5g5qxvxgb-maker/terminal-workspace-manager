# 🖥️ Terminal Workspace Manager

**Современный веб-интерфейс для управления терминальными сессиями с облачной синхронизацией**

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![Node.js](https://img.shields.io/badge/node-%3E%3D14.0.0-brightgreen)](https://nodejs.org/)

![Terminal Workspace Manager](https://img.shields.io/badge/version-2.0.0-blue)

Создавайте неограниченное количество рабочих столов, каждый с отдельной tmux сессией и мощным редактором заметок. Работайте с любого устройства - все данные синхронизируются автоматически!

## ✨ Возможности

### 📂 Система рабочих столов
- ✅ Неограниченное количество рабочих столов
- ✅ Каждый рабочий стол = отдельная tmux сессия с GitHub Copilot CLI
- ✅ Быстрое переключение между рабочими столами
- ✅ Галерея с миниатюрами внизу экрана
- ✅ Редактируемые названия рабочих столов
- ✅ Удаление рабочих столов (кнопка в header и в галерее)

### 🔍 Адаптивное масштабирование
- ✅ Zoom 50% - 200%
- ✅ Масштабируется весь контент (окно + шрифт)
- ✅ Индивидуальные настройки для каждого рабочего стола
- ✅ Кнопки управления масштабом: − / + / ↻

### 📝 Rich Text редактор заметок
- ✅ **Жирный**, *Курсив*, <u>Подчёркнутый</u>, ~~Зачёркнутый~~
- ✅ 4 размера шрифта (маленький, обычный, большой, огромный)
- ✅ Маркированные и нумерованные списки
- ✅ **Списки с галочками** (checklist) - отмечайте выполненные задачи
- ✅ Многоуровневые списки с отступами
- ✅ Автосохранение при вводе

### 🔄 Облачная синхронизация
- ✅ Синхронизация между всеми устройствами (телефон, компьютер, планшет)
- ✅ Автоматическое обновление каждые 3 секунды
- ✅ Индикатор синхронизации в реальном времени
- ✅ Двойное хранение (сервер + localStorage браузера)
- ✅ Работает офлайн с fallback на localStorage

### 🎨 Современный интерфейс
- ✅ Темная тема в стиле GitHub
- ✅ Плавные анимации и переходы
- ✅ Адаптивный дизайн (desktop, tablet, mobile)
- ✅ Font Awesome иконки
- ✅ Пульсирующий индикатор активности

## 🎬 Демо

```
┌─────────────────────────────────────────────────────────────┐
│  🖥️ Terminal Workspace Manager    ☁️ Синхронизировано    ➕  │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌───────────────────────────┬──────────────────────────┐  │
│  │ 🔴 Terminal - copilot-1   │  📝 Заметки              │  │
│  │ Zoom: − 100% + ↻ 🗑️       │  [Панель форматирования] │  │
│  ├───────────────────────────┤                          │  │
│  │                           │  • Мои задачи:           │  │
│  │  $ copilot                │  ☑️ Настроить систему    │  │
│  │  > Что ты хочешь сделать? │  ☐ Написать код          │  │
│  │                           │  ☐ Задеплоить            │  │
│  │  [Терминал здесь]         │                          │  │
│  │                           │  **Важно**: проверить    │  │
│  │                           │  перед релизом!          │  │
│  └───────────────────────────┴──────────────────────────┘  │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│ [📊 P2P Bot] [🏗️ DevOps] [📝 Notes] [💻 Project] [➕]      │
└─────────────────────────────────────────────────────────────┘
```

## 📦 Требования

- **OS**: Ubuntu 20.04+ (или другой Linux с systemd)
- **Node.js**: 14+ 
- **tmux**: любая версия
- **ttyd**: 1.6.0+
- **nginx**: 1.18+
- **GitHub Copilot CLI**: опционально (но рекомендуется)

## 🚀 Быстрая установка

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/YOUR_USERNAME/terminal-workspace-manager.git
cd terminal-workspace-manager

# 2. Запустите установщик
chmod +x install.sh
sudo ./install.sh

# 3. Откройте в браузере
# http://YOUR_SERVER_IP:8888
```

## 📋 Подробная установка

### Шаг 1: Установка зависимостей

```bash
# Обновите систему
sudo apt update && sudo apt upgrade -y

# Установите основные пакеты
sudo apt install -y tmux nginx nodejs npm git curl

# Установите ttyd
sudo apt install -y ttyd

# Или соберите из исходников для последней версии:
wget https://github.com/tsl0922/ttyd/releases/download/1.7.4/ttyd.x86_64
sudo mv ttyd.x86_64 /usr/local/bin/ttyd
sudo chmod +x /usr/local/bin/ttyd
```

### Шаг 2: Установка GitHub Copilot CLI (опционально)

```bash
# Установите через npm
npm install -g @githubnext/github-copilot-cli

# Авторизуйтесь
github-copilot-cli auth

# Создайте алиас copilot
echo 'eval "$(github-copilot-cli alias -- "$0")"' >> ~/.bashrc
source ~/.bashrc
```

### Шаг 3: Установка Terminal Workspace Manager

```bash
# Клонируйте репозиторий
git clone https://github.com/YOUR_USERNAME/terminal-workspace-manager.git
cd terminal-workspace-manager

# Создайте директории
sudo mkdir -p /var/www/terminal/data
sudo chmod 755 /var/www/terminal/data

# Скопируйте веб-файлы
sudo cp web/* /var/www/terminal/
sudo chown -R www-data:www-data /var/www/terminal

# Установите скрипты
sudo cp scripts/*.sh /usr/local/bin/
sudo chmod +x /usr/local/bin/*.sh

# Установите systemd сервисы
sudo cp systemd/*.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable terminal-manager workspace-sync terminal-titles
sudo systemctl start terminal-manager workspace-sync terminal-titles

# Настройте nginx
sudo cp nginx/terminal-manager.conf /etc/nginx/sites-available/terminal-manager

# Измените YOUR_SERVER_IP на ваш IP (опционально для ACL)
sudo nano /etc/nginx/sites-available/terminal-manager

# Активируйте конфигурацию
sudo ln -sf /etc/nginx/sites-available/terminal-manager /etc/nginx/sites-enabled/
sudo nginx -t && sudo systemctl reload nginx
```

### Шаг 4: Проверка установки

```bash
# Проверьте статус всех сервисов
sudo systemctl status terminal-manager workspace-sync terminal-titles

# Проверьте порты
sudo ss -tlnp | grep -E '768|8888|8889'

# Проверьте tmux сессии
tmux list-sessions

# Откройте в браузере
http://YOUR_SERVER_IP:8888
```

## 🎯 Использование

### Создание рабочего стола

1. Нажмите кнопку **"+ Новый рабочий стол"** в header
2. Или нажмите **"+ Создать рабочий стол"** в галерее внизу
3. Автоматически откроется терминал с GitHub Copilot CLI

### Переключение между рабочими столами

- Кликните на миниатюру в галерее внизу экрана
- Активный рабочий стол подсвечен зелёной рамкой
- Все рабочие столы сохраняют свое состояние

### Переименование рабочего стола

1. Кликните на название под миниатюрой в галерее
2. Введите новое название
3. Нажмите Enter или кликните вне поля

### Удаление рабочего стола

**Способ 1** (из галереи):
1. Наведите курсор на миниатюру
2. Нажмите кнопку **❌** в правом верхнем углу
3. Подтвердите удаление

**Способ 2** (из header):
1. Нажмите кнопку **🗑️** в header активного рабочего стола
2. Подтвердите удаление

### Масштабирование терминала

- **−** Уменьшить масштаб (шаг 10%)
- **+** Увеличить масштаб (шаг 10%)
- **↻** Сбросить на 100%
- Диапазон: 50% - 200%

### Работа с заметками

**Форматирование:**
1. Выделите текст
2. Используйте кнопки на панели инструментов
3. Доступны: жирный, курсив, подчёркнутый, зачёркнутый

**Списки:**
- Кнопка **≡** - маркированный список
- Кнопка **1.** - нумерованный список  
- Кнопка **☑** - список с галочками
- Кнопки **→** и **←** - отступы для многоуровневых списков

**Размер шрифта:**
- Выпадающий список с 4 размерами
- Маленький / Обычный / Большой / Огромный

### Синхронизация между устройствами

1. Откройте интерфейс на первом устройстве
2. Создайте и настройте рабочие столы
3. Откройте на втором устройстве - все данные появятся автоматически
4. Изменения синхронизируются в реальном времени (каждые 3 секунды)

## 📁 Структура проекта

```
terminal-workspace-manager/
├── web/
│   ├── index.html              # Основной веб-интерфейс
│   └── sync-server.js          # Node.js сервер синхронизации
├── scripts/
│   ├── start-terminal-services.sh      # Запуск терминалов
│   ├── stop-terminal-services.sh       # Остановка терминалов
│   └── update-terminal-titles.sh       # Обновление названий окон
├── systemd/
│   ├── terminal-manager.service        # Сервис терминалов
│   ├── workspace-sync.service          # Сервис синхронизации
│   └── terminal-titles.service         # Сервис обновления названий
├── nginx/
│   └── terminal-manager.conf          # Конфигурация nginx
├── docs/
│   └── [документация]
├── install.sh                  # Автоматический установщик
├── uninstall.sh               # Деинсталлятор
├── LICENSE                    # MIT лицензия
└── README.md                  # Этот файл
```

## 🔧 Конфигурация

### Изменение порта веб-интерфейса

Отредактируйте `/etc/nginx/sites-available/terminal-manager`:

```nginx
server {
    listen 8888;  # Измените на нужный порт
    ...
}
```

Перезагрузите nginx: `sudo systemctl reload nginx`

### Изменение частоты синхронизации

Отредактируйте `/var/www/terminal/index.html`, найдите:

```javascript
// Автосинхронизация каждые 3 секунды
setInterval(syncFromServer, 3000);  // Измените значение
```

### Изменение порта sync API

Отредактируйте `/var/www/terminal/sync-server.js`:

```javascript
const PORT = 8889;  // Измените на нужный порт
```

Не забудьте обновить proxy в nginx конфигурации!

## 🛠️ Управление

### Управление сервисами

```bash
# Статус всех сервисов
sudo systemctl status terminal-manager workspace-sync terminal-titles

# Перезапуск отдельного сервиса
sudo systemctl restart terminal-manager
sudo systemctl restart workspace-sync
sudo systemctl restart terminal-titles

# Остановка
sudo systemctl stop terminal-manager workspace-sync terminal-titles

# Запуск
sudo systemctl start terminal-manager workspace-sync terminal-titles

# Просмотр логов
sudo journalctl -u terminal-manager -f
sudo journalctl -u workspace-sync -f
sudo journalctl -u terminal-titles -f
```

### Работа с tmux сессиями

```bash
# Список всех сессий
tmux list-sessions

# Подключение к сессии
tmux attach -t terminal-1

# Отключение от сессии (без закрытия)
# Нажмите: Ctrl+B, затем D

# Убить сессию
tmux kill-session -t terminal-1

# Убить все сессии
tmux kill-server
```

### Резервное копирование

```bash
# Backup данных рабочих столов
sudo cp /var/www/terminal/data/workspaces.json ~/backup-$(date +%Y%m%d-%H%M).json

# Восстановление
sudo cp ~/backup-YYYYMMDD-HHMM.json /var/www/terminal/data/workspaces.json
sudo systemctl restart workspace-sync
```

## 🐛 Устранение неполадок

### Терминалы не запускаются

```bash
# 1. Проверьте статус сервиса
sudo systemctl status terminal-manager

# 2. Проверьте логи
sudo journalctl -u terminal-manager -n 50

# 3. Проверьте порты
sudo ss -tlnp | grep 768

# 4. Проверьте ttyd
which ttyd
ttyd --version

# 5. Перезапустите сервис
sudo systemctl restart terminal-manager
```

### Синхронизация не работает

```bash
# 1. Проверьте sync сервер
sudo systemctl status workspace-sync
curl http://localhost:8889/sync

# 2. Проверьте права на файлы
ls -la /var/www/terminal/data/
sudo chmod 755 /var/www/terminal/data

# 3. Проверьте логи
sudo journalctl -u workspace-sync -f

# 4. Проверьте nginx proxy
sudo nginx -t
curl http://localhost:8888/sync
```

### Nginx ошибки

```bash
# 1. Проверьте конфигурацию
sudo nginx -t

# 2. Проверьте логи
sudo tail -f /var/log/nginx/error.log
sudo tail -f /var/log/nginx/access.log

# 3. Проверьте порты
sudo ss -tlnp | grep nginx

# 4. Перезапустите nginx
sudo systemctl restart nginx
```

### Copilot не запускается

```bash
# 1. Проверьте установку
which github-copilot-cli
github-copilot-cli --version

# 2. Проверьте авторизацию
github-copilot-cli auth

# 3. Проверьте алиас
alias copilot

# 4. Если нет алиаса, добавьте в .bashrc
echo 'eval "$(github-copilot-cli alias -- "$0")"' >> ~/.bashrc
source ~/.bashrc
```

## 📊 Производительность

### Системные требования

- **CPU**: 2+ ядра (рекомендуется)
- **RAM**: 2GB+ (рекомендуется 4GB+)
- **Disk**: 500MB для системы + данные

### Потребление ресурсов

- **Память**: ~50MB на рабочий стол
- **CPU**: минимальное (только при активной работе в терминале)
- **Сеть**: ~1KB каждые 3 секунды для синхронизации
- **Диск**: ~100KB на рабочий стол с заметками

### Оптимизация

```bash
# Увеличьте частоту автосохранения (меньше запросов)
# в index.html измените:
setInterval(saveWorkspaces, 30000);  # с 10 на 30 секунд

# Уменьшите частоту синхронизации
setInterval(syncFromServer, 5000);  # с 3 на 5 секунд
```

## 🔒 Безопасность

### Рекомендации по безопасности

#### 1. Используйте Tailscale (рекомендуется)

```bash
# Установите Tailscale
curl -fsSL https://tailscale.com/install.sh | sh

# Запустите
sudo tailscale up

# Получите IP
tailscale ip -4
```

Затем настройте nginx ACL на Tailscale IP.

#### 2. Настройте firewall

```bash
# UFW
sudo ufw allow from YOUR_TAILSCALE_IP to any port 8888
sudo ufw enable

# iptables
sudo iptables -A INPUT -p tcp --dport 8888 -s YOUR_TAILSCALE_IP -j ACCEPT
sudo iptables -A INPUT -p tcp --dport 8888 -j DROP
```

#### 3. Настройте nginx ACL

В `/etc/nginx/sites-available/terminal-manager`:

```nginx
# Allow only specific IPs
allow 100.64.0.0/10;     # Tailscale network
allow 192.168.0.0/16;    # Your local network
deny all;
```

#### 4. Регулярно обновляйте

```bash
sudo apt update && sudo apt upgrade -y
npm update -g
```

#### 5. Мониторинг доступа

```bash
# Просматривайте логи доступа
sudo tail -f /var/log/nginx/access.log | grep terminal

# Настройте fail2ban (опционально)
sudo apt install fail2ban
```

## 🤝 Участие в разработке

Приветствуются Pull Requests! Для больших изменений сначала откройте Issue для обсуждения.

### Процесс разработки

1. Fork репозиторий
2. Создайте ветку feature (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

### Стиль кода

- JavaScript: используйте semicolons, camelCase
- HTML/CSS: 4 пробела отступ
- Bash: используйте shellcheck
- Комментарии на русском или английском

## 📝 Лицензия

MIT License - see [LICENSE](LICENSE) file for details

Copyright (c) 2025 Terminal Workspace Manager Contributors

## 🙏 Благодарности

- [ttyd](https://github.com/tsl0922/ttyd) - веб-терминалы
- [tmux](https://github.com/tmux/tmux) - управление сессиями
- [GitHub Copilot CLI](https://githubnext.com/projects/copilot-cli/) - AI помощник
- [Font Awesome](https://fontawesome.com/) - иконки
- [Nginx](https://nginx.org/) - веб-сервер
- [Node.js](https://nodejs.org/) - sync сервер

## 📞 Поддержка

- 💬 **Issues**: [GitHub Issues](https://github.com/YOUR_USERNAME/terminal-workspace-manager/issues)
- 📖 **Wiki**: [GitHub Wiki](https://github.com/YOUR_USERNAME/terminal-workspace-manager/wiki)
- 📧 **Email**: support@example.com

## 🗺️ Roadmap

- [ ] Темы интерфейса (светлая/темная/кастомная)
- [ ] Экспорт/импорт рабочих столов в JSON
- [ ] Поддержка плагинов и расширений
- [ ] Встроенный файловый менеджер
- [ ] Мультиюзерность с авторизацией
- [ ] Docker образ для быстрой установки
- [ ] Kubernetes Helm chart
- [ ] SSH туннелирование
- [ ] Поддержка WebRTC для P2P синхронизации
- [ ] Мобильное приложение (PWA)

## 📈 Статистика

![GitHub stars](https://img.shields.io/github/stars/YOUR_USERNAME/terminal-workspace-manager)
![GitHub forks](https://img.shields.io/github/forks/YOUR_USERNAME/terminal-workspace-manager)
![GitHub issues](https://img.shields.io/github/issues/YOUR_USERNAME/terminal-workspace-manager)
![GitHub license](https://img.shields.io/github/license/YOUR_USERNAME/terminal-workspace-manager)

---

**Made with ❤️ for developers who love terminals**

**Разработано с ❤️ для разработчиков, которые любят терминалы**
