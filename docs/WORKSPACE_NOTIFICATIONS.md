# 🔔 Уведомления на миниатюрах рабочих столов

## ✅ Что добавлено

### Визуальные уведомления
- 🔴 Красный значок с пульсацией - требуется внимание
- 🟢 Зелёный значок - процесс завершён успешно
- 🟡 Жёлтый значок - предупреждение

### Автоматика
- Уведомление **автоматически исчезает** при переключении на рабочий стол
- Уведомления **сохраняются** при синхронизации между устройствами
- **Пульсирующая анимация** привлекает внимание

## 🎯 Как использовать

### 1. Через JavaScript консоль браузера (F12)

```javascript
// Показать предупреждение на рабочем столе 1
notifyWorkspace(1, 'warning', '!');

// Показать успех на рабочем столе 2
notifySuccess(2);

// Показать ошибку на рабочем столе 3
notifyError(3);

// Убрать уведомление
clearNotify(1);

// Кастомные иконки
notifyWorkspace(1, 'warning', '⚠');
notifyWorkspace(2, 'success', '✓');
notifyWorkspace(3, '', '?');
```

### 2. Из терминала через API (планируется)

```bash
# В ~/.bashrc добавить:
export WORKSPACE_ID=1  # Номер вашего рабочего стола

# Функция для уведомления при завершении команды
notify_done() {
    "$@"
    local status=$?
    if [ $status -eq 0 ]; then
        echo "✅ Команда выполнена успешно"
        # Здесь будет API вызов для уведомления
    else
        echo "❌ Команда завершилась с ошибкой"
        # Здесь будет API вызов для уведомления
    fi
    return $status
}

# Использование:
notify_done npm install
notify_done ./build.sh
```

### 3. Автоматическое обнаружение (в разработке)

Планируется автоматическое обнаружение:
- Приглашений для ввода (`[Y/n]`, `Press Enter`)
- Завершения долгих процессов
- Ошибок в терминале
- Важных сообщений

## 🎨 Типы уведомлений

### Стандартные:
- `type: ''` (пусто) - красный, внимание/ошибка
- `type: 'warning'` - жёлтый, предупреждение
- `type: 'success'` - зелёный, успех

### Иконки:
- `!` - внимание
- `✓` - успех
- `✗` - ошибка
- `?` - вопрос
- `⚠` - предупреждение
- `⏱` - ожидание
- Любой emoji или символ

## 📖 Примеры использования

### Сценарий 1: Уведомление при завершении сборки
```javascript
// Перед началом сборки
console.log('Starting build...');

// После завершения (вручную или через скрипт)
notifySuccess(1);  // Рабочий стол 1
```

### Сценарий 2: Требуется подтверждение
```javascript
// Когда скрипт запрашивает подтверждение
notifyWorkspace(2, 'warning', '?');
```

### Сценарий 3: Ошибка в процессе
```javascript
// При возникновении ошибки
notifyError(3);
```

### Сценарий 4: Мониторинг с разных устройств
1. Запустили долгий процесс на десктопе
2. Установили уведомление: `notifyWorkspace(1, 'warning', '⏱')`
3. Ушли с телефоном
4. Когда процесс завершится, зайдёте с телефона - увидите уведомление!

## 🔧 Настройка автоматических уведомлений

### Bash prompt hook (пример)
```bash
# Добавьте в ~/.bashrc

# Функция для отправки уведомления
send_workspace_notify() {
    local workspace_id="${WORKSPACE_ID:-1}"
    local type="${1:-warning}"
    local icon="${2:-!}"
    
    # Через JavaScript в браузере (нужно расширение или API)
    # Временное решение: вручную через консоль
    echo "Уведомление: workspace=$workspace_id type=$type icon=$icon"
}

# Хук на завершение команды
_notify_command_done() {
    local status=$?
    if [ $status -eq 0 ]; then
        # Успех
        true  # Здесь будет send_workspace_notify "success" "✓"
    else
        # Ошибка
        true  # Здесь будет send_workspace_notify "" "✗"
    fi
}

# Раскомментируйте для активации:
# PROMPT_COMMAND="_notify_command_done; $PROMPT_COMMAND"
```

## 🚀 Будущие возможности

- [ ] API endpoint для установки уведомлений извне
- [ ] Webhook для уведомлений
- [ ] Автоматическое обнаружение паттернов в терминале
- [ ] Интеграция с GitHub Actions (уведомление при завершении CI/CD)
- [ ] Звуковые уведомления (опционально)
- [ ] Desktop notifications через браузер
- [ ] История уведомлений
- [ ] Настраиваемые правила для уведомлений

## 💡 Советы

1. **Используйте разные иконки** для разных типов уведомлений
2. **Устанавливайте уведомления** перед долгими процессами
3. **Очищайте уведомления** когда обработали их
4. **Используйте warning для вопросов** - они жёлтые и хорошо видны
5. **Используйте success для завершения** - зелёный = всё ОК

## 📸 Примеры

```javascript
// Компиляция завершена
notifySuccess(1);

// Требуется ваш ввод
notifyWorkspace(2, 'warning', '?');

// Произошла ошибка
notifyError(3);

// Процесс ожидает
notifyWorkspace(4, 'warning', '⏱');

// Кастомное сообщение
notifyWorkspace(5, 'success', '🎉');
```

## 🔗 Синхронизация

Уведомления **автоматически синхронизируются** между всеми вашими устройствами:
- Установили на компьютере → видно на телефоне
- Очистили на телефоне → исчезло на компьютере

---

**Теперь вы никогда не пропустите важные события в терминале!** 🎉
